
Final Code:

SSEG	SEGMENT
SSEG	ENDS
DSEG	SEGMENT
DSEG	ENDS
CSEG	SEGMENT
		ASSUME	CS:CSEG,DS:DSEG
		ASSUME	SS:SSEG
LABEL0:	MOV		AX,DSEG
		MOV		DS,AX
		MOV		AX,0
		MOV		BX,0
		MOV		[BX],AX
		MOV		AX,1
		CMP		AX,2
		JG		LABEL1
		MOV		AX,0
		JMP		LABEL2
LABEL1:	MOV		AX,1
LABEL2:	MOV		BX,2
		MOV		DX,0
		CMP		[BX],DX
		JZ		LABEL3
		MOV		AX,1
		MOV		BX,0
		MOV		[BX],AX
LABEL3:	NOP		
CSEG	ENDS
		END	LABEL0
